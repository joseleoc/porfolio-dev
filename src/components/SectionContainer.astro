---
interface Props {
  class?: string;
  id: string;
}
const { class: className, id } = Astro.props;
---

<section id={id} class={`mx-auto w-full lg:max-w-[740px] ${className}`}>
  <slot />
</section>
